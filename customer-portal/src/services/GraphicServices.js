//import fetchData from "./fetchData";

/*import { useFetch } from "./useFetch";

const baseURL = process.env.REACT_APP_WEB_SERVICES;
//const apiDataGraphicList = fetchData(baseURL + "test.php");

export default function getAllGraphics(userId) {
  const { graphics, isLoading, error } = useFetch(baseURL + "test.php");

  return { graphics, isLoading, error };
}

 export default function fetchData(url) {
  const promise = fetch(url)
    .then((response) => response.json())
    .then((data) => data);

  return getSuspender(promise);
}
 */
